from setuptools import setup, find_packages

setup(
    name="crec",
    version="0.0.1",
    description="A Python library for downloading and parsing Congressional Record data",
    long_description_content_type="text/markdown"
)